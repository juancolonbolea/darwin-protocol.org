---
axiom: "A2,A8"
spec_commit: 6f8ea84
original_verdict: "UNDERSPECIFIED (A2 high, A8 medium)"
verify_verdict: MODIFIED
confidence: high
---

Both cards' UNDERSPECIFIED holds. Two findings change.

**1. Estimability is the right core; the "externally validated" routing is wrong.** A4+A7 do not carry that conjunct — T42 does, and T42 derives from A2. A2 therefore states what its own downstream theorem proves, making T42 analytic: its proof quotes the conjunct as a premise, then re-derives it. Fix by deletion, not relocation — the proof then derives genuinely from A4 (overflow exists), A13 (results public) and a thin A2. Promoting external validation to the core instead leaves the estimator unstated, empties T42, and collides with A1. Deletion also dissolves the A16←A15←T42←A2 circle the A2 card charged.

**2. A8's circularity is textual, and its "residue: none" self-defeats.** The circle is three clauses: P2's enabling condition, A8's grounding in P2, A2's manufacturing clause. No proof cites P2's enabling clause, so deleting it is free. But once deleted — as the A8 card itself demands — comparability is no longer carried by P2 and does need axiom status. The de-duplication was an artifact of the defect the card asks to remove. Containment is bidirectional: A8's informativeness enrichment and its "the Quote is not a sufficient statistic" are estimator-validity claims sitting in A8's text.

**Clean split, no shared conjunct.**

- **A2 — Merit is estimable.** A protocol-computable estimator of one agent's contribution exists, separating it from luck, beta and access at finite sample, and informative when the agent knows it is computed. Per-agent; validity.
- **A8 — Merit is commensurable.** Per-agent estimates admit a common ordering across heterogeneous agents. Cross-agent; comparison. Not implied by validity.

Out of both: independence (A9+P6), determinism (governance parameter), publication (A13), attribution (A15+A16), external validation (T42). Strip "certified by the protocol" from P1.

**§III: nothing breaks; six citation repairs.** T54's A8 → A2+A8 (informativeness is estimator-side; observability is A9). T44's "every touchpoint" → A15+A16+P5. T43's shared-mechanism step re-sourced. T33 and T47/T56 need A8. T53's axiom row omits the A8 the dependency graph already lists — pre-existing. F6 and F9 → A2+A8; F9 needs both conjuncts (optimisation against the estimator, plus one ordering for all), the strongest evidence they are two axioms.
